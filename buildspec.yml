version: 0.2

phases:
  install:
    commands:
      #- git clone https://github.com/byu-oit/lambda_utils.git utils
      pip install -r requirements.txt -t .
  pre_build:
    commands:
      #TODO enable the tests
      #- python ical2slackstatus.py test --verbose True
  build:
    commands:
      #- echo "Nothing to run in the build phase..."
  post_build:
    commands:
      #- echo "Nothing to run in the post_build phase..."
artifacts:
  files:
    - '**/*'
  discard-paths: no